name: GDS XOR
on:
  pull_request:
    paths:
      - "**.gds"

jobs:
  gds-xor:
    permissions:
      contents: read
      pull-requests: write
    uses: doplaydo/pdk-ci-workflow/.github/workflows/gds-xor.yml@main
    secrets:
      PDK_CI_WORKFLOW_TOKEN: ${{ secrets.PDK_CI_WORKFLOW_TOKEN }}
